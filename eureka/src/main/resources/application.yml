spring:
  application:
    name: eureka-server

server:
  port: 8761

eureka:
  instance:
    hostname: ${IP_EUREKA_SERVER:eureka-server}
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false
